# =====================================================
#  HotelOS — Variables de Entorno (Supabase)
# =====================================================

# Supabase → Settings → Database → Connection string → "URI"
DATABASE_URL="postgresql://postgres.xxxx:PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Supabase → Settings → Database → Connection string → "Direct connection"
DIRECT_URL="postgresql://postgres.xxxx:PASSWORD@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# JWT Secret — cambia esto por una cadena larga y segura
JWT_SECRET="hotelos_jwt_super_secreto_cambia_esto_2024"

# Puerto local
PORT=3000

NODE_ENV=production
